# Python prerequisites for RepoGhost
#
# Install with:
#   python -m pip install -r requirements.txt
#
# Note:
# - Bun is still required separately for the Jac client/Vite pipeline.
# - After installing these Python requirements, run:
#     jac install
#     jac check main.jac
#     jac start --dev main.jac

jaclang==0.13.5
jac-client==0.3.11
byllm==0.6.0
websockets  # required by uvicorn for WebSocket support
